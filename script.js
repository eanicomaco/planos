// var screenWidth = window.innerWidth;
// var containerWidth = screenWidth * 0.9;
// var divWidth = (containerWidth - 30) / 3; // Assume 15px spacing

// if (screenWidth >= 768) { // Assume 768px as the breakpoint for mobile devices
//     var divs = document.querySelectorAll('.cardPlanos');
//     divs.forEach(function(div) {
//         div.style.width = divWidth + 'px';
//     });
// }